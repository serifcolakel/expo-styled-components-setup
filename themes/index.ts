import { colors, colorPalette } from "./colors";
import { inputSizes, sizes } from "./sizes";
import { lineHeights, spacing } from "./spacing";
import {
  align,
  borderWidth,
  fontSizes,
  justify,
  radius,
  weights,
  opacity,
} from "./styles";

const theme = {
  colors: colors.light,
  sizes,
  spacing,
  lineHeights,
  fontSizes,
  radius,
  weights,
  align,
  justify,
  inputSizes,
  borderWidth,
  opacity,
  colorPalette,
  isDark: false,
};

export default theme;
